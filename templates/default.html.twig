{% extends 'partials/base.html.twig' %}

{% block content %}
<div class="container">
    <div class="columns is-centered">
        <div class="column is-7">

            <div class="content">
                <h1>
                    {{ page.title }}
                </h1>
                {{ page.content }}
            </div>

        </div>
    </div>
</div>
{% endblock %}
